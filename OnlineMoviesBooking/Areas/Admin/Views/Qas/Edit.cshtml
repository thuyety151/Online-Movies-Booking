@using Microsoft.EntityFrameworkCore;
@using OnlineMoviesBooking.Models.Models;

@model OnlineMoviesBooking.Models.Models.Qa

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Qa</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            @{
               // CinemaContext context = new CinemaContext();
                //var account = context.Account.FromSqlRaw($"EXEC dbo.USP_GetDetailAccount @id = '{Model.IdAccount}'").ToList();
            }
            <div class="form-group">
                <input asp-for="IdAccount" class="form-control" hidden />
                <span asp-validation-for="IdAccount" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Tên</label>
                @*<input class="form-control" value="@account[0].Name" readonly />*@
            </div>
            <div class="form-group">
                <label asp-for="Email" class="control-label"></label>
                <input asp-for="Email" class="form-control" readonly />
                <span asp-validation-for="Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label class="control-label">Thời gian</label>
                <input asp-for="Time" class="form-control" />
                <span asp-validation-for="Time" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Content" class="control-label"></label>
                <textarea asp-for="Content" class="form-control" placeholder="Nhập nội dung"></textarea>
                <span asp-validation-for="Content" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

